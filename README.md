# qr
Offline, single-file QR code generator with full styling: dot shapes, colors, gradients, corner styles, and logo embedding. Supports vCard contacts (with .vcf import and Arabic/UTF-8), URLs, plain text, WiFi auto-connect, email, and SMS. No server, no dependencies, just one HTML file.
